import React from 'react';

export default class ListModeButton extends React.PureComponent {
  constructor(props) {
    super(props);
  }

  render() {
    return (<div>MyComponent</div>);
  }
}

ListModeButton.propTypes = {
};
