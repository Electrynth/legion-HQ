import React from 'react';
import Helmet from 'react-helmet';

// {`${faction} | Legion Builder`}

const Title = ({ faction }) => (
  <Helmet>
    <title>
      Title
    </title>
  </Helmet>
);

export default Title;
